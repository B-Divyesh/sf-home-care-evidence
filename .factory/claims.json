[
  {
    "id": "card-records",
    "claim": "Cards keep the finding, completed-work notes, attachments, repeat interval, and next due date together.",
    "where": "home product UI, README",
    "test": "npm run test:e2e -- --grep @claim:card-records",
    "sandbox": "fresh browser; create a complete card, reload it, add work, and inspect the updated history"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit.",
    "where": "home first screen, README",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "fresh demo browser; wait for service-worker control, go offline, reload, and assert shell plus sample record"
  },
  {
    "id": "demo-isolation",
    "claim": "Demo changes stay in a separate sample logbook and are discarded when the visitor starts for real.",
    "where": "demo banner, README",
    "test": "npm run test:e2e -- --grep @claim:demo-isolation",
    "sandbox": "seed a real IndexedDB record, enter demo, assert it is hidden, leave demo, and assert the real record is intact"
  },
  {
    "id": "recurrence-latest",
    "claim": "The latest completed work sets the next due date.",
    "where": "How it works, README",
    "test": "npm run test:e2e -- --grep @claim:recurrence-latest",
    "sandbox": "demo record; add a later completed-work entry and assert the displayed due date and history count"
  },
  {
    "id": "search-filter",
    "claim": "The logbook can find cards by text and recover from an empty result.",
    "where": "product UI, README",
    "test": "npm run test:e2e -- --grep @claim:search-filter",
    "sandbox": "fresh demo; filter to one sample card, force no results, clear filters, and recover all sample cards"
  },
  {
    "id": "local-privacy",
    "claim": "Records stay on this device unless exported, with no analytics, ads, trackers, or third-party runtime scripts.",
    "where": "home first screen, privacy, README",
    "test": "npm run test:e2e -- --grep @claim:local-privacy",
    "sandbox": "capture every request through a demo workflow and assert only the product origin plus the isolated demo database"
  },
  {
    "id": "open-export",
    "claim": "Open JSON exports contain every note and attachment.",
    "where": "data dialog, privacy, README",
    "test": "npm run test:e2e -- --grep @claim:open-export",
    "sandbox": "download the fresh demo archive and assert all three records, a known note, and both named attachments"
  },
  {
    "id": "print-history",
    "claim": "Print one-page history prepares the selected card and its full history.",
    "where": "card action, README",
    "test": "npm run test:e2e -- --grep @claim:print-history",
    "sandbox": "fresh demo; invoke print and assert exactly one print target with expanded history"
  },
  {
    "id": "free-limit",
    "claim": "The free logbook holds 8 cards; a $29 one-time Unlimited license removes the card limit.",
    "where": "home first screen, data dialog, terms, README",
    "test": "npm run test:e2e -- --grep @claim:free-limit",
    "sandbox": "fresh demo seeded to eight cards; attempt a ninth and assert the limit message and exact paid offer"
  },
  {
    "id": "encrypted-archive",
    "claim": "Unlimited creates AES-GCM-256 archives using PBKDF2-SHA256 with 250,000 iterations and a passphrase.",
    "where": "data dialog, privacy, README",
    "test": "npm run test:e2e -- --grep @claim:encrypted-archive",
    "sandbox": "fresh licensed demo; download the encrypted archive, inspect its format, and assert sample notes are not plaintext"
  },
  {
    "id": "license-cache",
    "claim": "A saved license is verified at most once per day.",
    "where": "privacy, README",
    "test": "npm run test:e2e -- --grep @claim:license-cache",
    "sandbox": "fresh demo with a current cached verdict; intercept verification and assert zero network calls"
  }
]
